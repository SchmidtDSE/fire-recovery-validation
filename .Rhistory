df_filtered = df %>%
select(fire_name, fire_days, geom) %>%
st_make_valid()
calfire = st_read('Validation_Fire_Perimeters_2015_2024.shp') %>%
select(FIRE_NAME, geometry) %>%
rename(fire_name = FIRE_NAME) %>%
st_make_valid() %>%
st_transform(crs = st_crs(df_filtered))
calculate_intersection = function(i) {
row = df_filtered[i, ]
cal_poly = calfire %>% filter(fire_name == row$fire_name)
if (nrow(cal_poly) == 0) return(NULL)
intersection = st_intersection(row, cal_poly)
# Handle case where there's no intersection
if (nrow(intersection) == 0 || all(st_is_empty(intersection))) {
return(data.frame(
fire_name = row$fire_name,
fire_days = row$fire_days,
percent_overlap = 0
))
data.frame(
fire_name = row$fire_name,
fire_days = row$fire_days,
percent_overlap = pct)
}
# Sum all intersection areas (in case of multipart geometries)
intersection_area = sum(st_area(intersection))
calfire_area = sum(st_area(cal_poly))
pct = as.numeric(intersection_area / calfire_area * 100)
if (pct > 100) {
pct = 999
print(paste("Warning: Over 100% overlap for", row$fire_name))
}
}
# Calculate overlap for each fire/fire_days combination
overlap_results = map_dfr(1:nrow(df_filtered), calculate_intersection()) %>%
crossing(metric = c("dnbr", "rdnbr"))
calculate_intersection = function(i) {
row = df_filtered[i, ]
cal_poly = calfire %>% filter(fire_name == row$fire_name)
if (nrow(cal_poly) == 0) return(NULL)
intersection = st_intersection(row, cal_poly)
# Handle case where there's no intersection
if (nrow(intersection) == 0 || all(st_is_empty(intersection))) {
return(data.frame(
fire_name = row$fire_name,
fire_days = row$fire_days,
metric = row$metric,
percent_overlap = 0
))
}
# Sum all intersection areas (in case of multipart geometries)
intersection_area = sum(st_area(intersection))
calfire_area = sum(st_area(cal_poly))
pct = min(100, as.numeric(intersection_area / calfire_area * 100))
data.frame(
fire_name = row$fire_name,
fire_days = row$fire_days,
metric = row$metric,
percent_overlap = pct
)
}
overlap_results = map_dfr(1:nrow(df_filtered), calculate_intersection)
i = 1
row = df_filtered[i, ]
cal_poly = calfire %>% filter(fire_name == row$fire_name)
if (nrow(cal_poly) == 0) return(NULL)
intersection = st_intersection(row, cal_poly)
intersection
# Handle case where there's no intersection
if (nrow(intersection) == 0 || all(st_is_empty(intersection))) {
return(data.frame(
fire_name = row$fire_name,
fire_days = row$fire_days,
metric = row$metric,
percent_overlap = 0
))
}
# Sum all intersection areas (in case of multipart geometries)
intersection_area = sum(st_area(intersection))
calfire_area = sum(st_area(cal_poly))
pct = min(100, as.numeric(intersection_area / calfire_area * 100))
pct
data.frame(
fire_name = row$fire_name,
fire_days = row$fire_days,
metric = row$metric,
percent_overlap = pct
)
data.frame(
fire_name = row$fire_name,
fire_days = row$fire_days,
metric = row$metric,
percent_overlap = pct
)
fire_name = row$fire_name
fire_days = row$fire_days
metric = row$metric
percent_overlap = pct
row
df_filtered = df %>%
select(fire_name, fire_days, metric, geometry) %>%
st_make_valid()
df_filtered = df %>%
select(fire_name, fire_days, metric, geom) %>%
st_make_valid()
row = df_filtered[i, ]
data.frame(
fire_name = row$fire_name,
fire_days = row$fire_days,
metric = row$metric,
percent_overlap = pct
)
calculate_intersection = function(i) {
row = df_filtered[i, ]
cal_poly = calfire %>% filter(fire_name == row$fire_name)
if (nrow(cal_poly) == 0) return(NULL)
intersection = st_intersection(row, cal_poly)
# Handle case where there's no intersection
if (nrow(intersection) == 0 || all(st_is_empty(intersection))) {
return(data.frame(
fire_name = row$fire_name,
fire_days = row$fire_days,
metric = row$metric,
percent_overlap = 0
))
}
# Sum all intersection areas (in case of multipart geometries)
intersection_area = sum(st_area(intersection))
calfire_area = sum(st_area(cal_poly))
pct = min(100, as.numeric(intersection_area / calfire_area * 100))
data.frame(
fire_name = row$fire_name,
fire_days = row$fire_days,
metric = row$metric,
percent_overlap = pct
)
}
overlap_results = map_dfr(1:nrow(df_filtered), calculate_intersection)
ggplot() + theme_bw() +
geom_boxplot(data = overlap_results, aes(x = factor(metric), y = percent_overlap, fill = metric))
row = df_filtered[i, ]
cal_poly = calfire %>% filter(fire_name == row$fire_name)
if (nrow(cal_poly) == 0) return(NULL)
intersection = st_intersection(row, cal_poly)
# Handle case where there's no intersection
if (nrow(intersection) == 0 || all(st_is_empty(intersection))) {
return(data.frame(
fire_name = row$fire_name,
fire_days = row$fire_days,
metric = row$metric,
percent_overlap = 0
))
}
# Sum all intersection areas (in case of multipart geometries)
intersection_area = sum(st_area(intersection))
calfire_area = sum(st_area(cal_poly))
pct = min(100, as.numeric(intersection_area / calfire_area * 100))
data.frame(
fire_name = row$fire_name,
fire_days = row$fire_days,
metric = row$metric,
percent_overlap = pct
)
i = 2
row = df_filtered[i, ]
cal_poly = calfire %>% filter(fire_name == row$fire_name)
if (nrow(cal_poly) == 0) return(NULL)
intersection = st_intersection(row, cal_poly)
# Handle case where there's no intersection
if (nrow(intersection) == 0 || all(st_is_empty(intersection))) {
return(data.frame(
fire_name = row$fire_name,
fire_days = row$fire_days,
metric = row$metric,
percent_overlap = 0
))
}
# Sum all intersection areas (in case of multipart geometries)
intersection_area = sum(st_area(intersection))
calfire_area = sum(st_area(cal_poly))
pct = min(100, as.numeric(intersection_area / calfire_area * 100))
data.frame(
fire_name = row$fire_name,
fire_days = row$fire_days,
metric = row$metric,
percent_overlap = pct
)
i = 1
row = df_filtered[i, ]
row
View(df)
View(df)
###
df_variance = df %>%
select(fire_name, fire_days, metric, calfire_var) %>%
pivot_wider(names_from = metric, values_from = calfire_var)
View(df_variance)
###
df_variance = df %>%
select(fire_name, fire_days, metric, calfire_var) %>%
pivot_wider(names_from = metric, values_from = calfire_var)
###
df_variance = df %>%
select(fire_name, fire_days, metric, calfire_var) %>%
pivot_wider(names_from = metric, values_from = calfire_var)
View(df_variance)
head(df_variance)
###
df_variance = df %>%
select(fire_name, fire_days, metric, calfire_var) %>%
sf::st_drop_geometry() %>%
pivot_wider(names_from = metric, values_from = calfire_var)
head(df_variance)
ggplot() + theme_bw() +
geom_point(data = df, aes(color = fire_days, x =  dnbr, y = rdnbr))
ggplot() + theme_bw() +
geom_point(data = df_variance, aes(color = fire_days, x =  dnbr, y = rdnbr))
summary(df)
hist(df$calfire_mean)
hist(df$calfire_mean)
hist(df$calfire_var)
hist(df$filtered_mean)
summary(df[df$metric,] == 'dbnr')
summary(df[df$metric == 'dbnr',])
summary(df[df$metric == 'rdbnr',])
df[df$metric == 'dbnr',]
summary(df[df$metric == 'dnbr',])
summary(df[df$metric == 'rdnbr',])
View(df)
df_test = sf::st_drop_geometry(df)
ggplot() + theme_bw() +
geom_histogram(data = df_test, aes(x = calfire_mean, color = fire_days)) +
facet_wrap(~metric, scales = "free")
ggplot() + theme_bw() +
geom_histogram(data = df_test, aes(x = calfire_mean, fill = fire_days)) +
facet_wrap(~metric, scales = "free")
ggplot() + theme_bw() +
geom_histogram(data = df_test, aes(x = calfire_mean, group = fire_days, fill = fire_days)) +
facet_wrap(~metric, scales = "free")
ggplot() + theme_bw() +
geom_histogram(data = df_test, aes(x = calfire_mean, group = fire_days, fill = fire_days)) +
facet_wrap(~metric, scales = "free") +
scale_fill_scico_d(palette = "lajolla")
ggplot() + theme_bw() +
geom_histogram(data = df_test, aes(x = calfire_mean, group = fire_days, fill = fire_days)) +
facet_wrap(~metric, scales = "free") +
scale_fill_scico(palette = "lajolla")
ggplot() + theme_bw() +
geom_histogram(data = df_test, aes(x = calfire_mean, group = fire_days, fill = fire_days)) +
facet_wrap(~metric, scales = "free") +
scale_fill_scico(palette = "baltlow")
ggplot() + theme_bw() +
geom_histogram(data = df_test, aes(x = calfire_mean, group = fire_days, fill = fire_days)) +
facet_wrap(~metric, scales = "free") +
scale_fill_scico(palette = "batlow")
ggplot() + theme_bw() +
geom_histogram(data = df_test, aes(x = calfire_mean, group = fire_days, fill = fire_days)) +
facet_wrap(~metric) +
scale_fill_scico(palette = "batlow")
ggplot() + theme_bw() +
geom_histogram(data = df_test, aes(x = calfire_mean, group = fire_days, fill = fire_days)) +
facet_wrap(~metric, ncol = 1) +
scale_fill_scico(palette = "batlow")
ggplot() + theme_bw() +
geom_histogram(data = df_test, aes(x = calfire_mean, group = fire_days, fill = fire_days), breaks = 100) +
facet_wrap(~metric, ncol = 1) +
scale_fill_scico(palette = "batlow")
ggplot() + theme_bw() +
geom_histogram(data = df_test, aes(x = calfire_mean, group = fire_days, fill = fire_days),bins = 100) +
facet_wrap(~metric, ncol = 1) +
scale_fill_scico(palette = "batlow")
ggplot() + theme_bw() +
geom_histogram(data = df_test, aes(x = calfire_mean, group = fire_days, fill = fire_days),bins = 100) +
facet_wrap(~metric, ncol = 1, scales.x = 'free') +
scale_fill_scico(palette = "batlow")
ggplot() + theme_bw() +
geom_histogram(data = df_test, aes(x = calfire_mean, group = fire_days, fill = fire_days),bins = 100) +
facet_wrap(~metric, ncol = 1, scales = 'free') +
scale_fill_scico(palette = "batlow")
View(df_test)
df_issues = df_test %>%
filter(abs(calfire_mean) > 1 | abs(filtered_mean) > 1)
View(df_issues)
df_dnbr = st_read("validation_metrics_dnbr.gpkg")
df_rdnbr = st_read("validation_metrics_rdnbr.gpkg")
View(df_dnbr)
calculate_intersection = function(i) {
row = df_filtered[i, ]
cal_poly = calfire %>% filter(fire_name == row$fire_name)
if (nrow(cal_poly) == 0) return(NULL)
intersection = st_intersection(row, cal_poly)
# Handle case where there's no intersection
if (nrow(intersection) == 0 || all(st_is_empty(intersection))) {
return(data.frame(
fire_name = row$fire_name,
fire_days = row$fire_days,
metric = row$metric,
percent_overlap = 0
))
}
# Sum all intersection areas (in case of multipart geometries)
intersection_area = sum(st_area(intersection))
calfire_area = sum(st_area(cal_poly))
pct = min(100, as.numeric(intersection_area / calfire_area * 100))
data.frame(
fire_name = row$fire_name,
fire_days = row$fire_days,
metric = row$metric,
percent_overlap = pct
)
}
overlap_results_dnbr = map_dfr(1:nrow(df_filtered), calculate_intersection)
View(overlap_results_dnbr)
calculate_intersection = function(i) {
row = df_filtered[i, ]
cal_poly = calfire %>% filter(fire_name == row$fire_name)
if (nrow(cal_poly) == 0) return(NULL)
intersection = st_intersection(row, cal_poly)
# Handle case where there's no intersection
if (nrow(intersection) == 0 || all(st_is_empty(intersection))) {
return(data.frame(
fire_name = row$fire_name,
fire_days = row$fire_days,
percent_overlap = 0
))
}
# Sum all intersection areas (in case of multipart geometries)
intersection_area = sum(st_area(intersection))
calfire_area = sum(st_area(cal_poly))
pct = min(100, as.numeric(intersection_area / calfire_area * 100))
data.frame(
fire_name = row$fire_name,
fire_days = row$fire_days,
metric = row$metric,
percent_overlap = pct
)
}
overlap_results_dnbr = map_dfr(1:nrow(df_filtered), calculate_intersection) %M%
mutate(metric = 'dnbr')
overlap_results_dnbr = map_dfr(1:nrow(df_filtered), calculate_intersection) %>%
mutate(metric = 'dnbr')
# area overlap
df_filtered_dnbr = df_dnbr %>%
select(fire_name, fire_days, metric, geom) %>%
st_make_valid()
overlap_results_dnbr = map_dfr(1:nrow(df_filtered), calculate_intersection) %>%
mutate(metric = 'dnbr')
df_filtered_ednbr = df_rdnbr %>%
select(fire_name, fire_days, metric, geom) %>%
st_make_valid()
setwd("~/Desktop/NPS/Fireseverity/Validation")
library(tidyverse)
library(scico)
library(sf)
df_dnbr = st_read("validation_metrics_dnbr.gpkg")
df_rdnbr = st_read("validation_metrics_rdnbr.gpkg")
calfire = st_read('Validation_Fire_Perimeters_2015_2024.shp') %>%
select(FIRE_NAME, geometry) %>%
rename(fire_name = FIRE_NAME) %>%
st_make_valid() %>%
st_transform(crs = st_crs(df_filtered))
calculate_intersection = function(i) {
row = df_filtered[i, ]
cal_poly = calfire %>% filter(fire_name == row$fire_name)
if (nrow(cal_poly) == 0) return(NULL)
intersection = st_intersection(row, cal_poly)
# Handle case where there's no intersection
if (nrow(intersection) == 0 || all(st_is_empty(intersection))) {
return(data.frame(
fire_name = row$fire_name,
fire_days = row$fire_days,
percent_overlap = 0
))
}
# Sum all intersection areas (in case of multipart geometries)
intersection_area = sum(st_area(intersection))
calfire_area = sum(st_area(cal_poly))
pct = min(100, as.numeric(intersection_area / calfire_area * 100))
data.frame(
fire_name = row$fire_name,
fire_days = row$fire_days,
metric = row$metric,
percent_overlap = pct
)
}
# area overlap
df_filtered_dnbr = df_dnbr %>%
select(fire_name, fire_days, metric, geom) %>%
st_make_valid()
overlap_results_dnbr = map_dfr(1:nrow(df_filtered), calculate_intersection) %>%
mutate(metric = 'dnbr')
overlap_results_dnbr = map_dfr(1:nrow(df_filtered_dnbr), calculate_intersection) %>%
mutate(metric = 'dnbr')
df_filtered_rdnbr = df_rdnbr %>%
select(fire_name, fire_days, metric, geom) %>%
st_make_valid()
# area overlap
df_filtered_dnbr = df_dnbr %>%
select(fire_name, fire_days, metric, geom) %>%
st_make_valid()
overlap_results_dnbr = map_dfr(1:nrow(df_filtered_dnbr), calculate_intersection) %>%
mutate(metric = 'dnbr')
df_dnbr = st_read("validation_metrics_dnbr.gpkg")
df_rdnbr = st_read("validation_metrics_rdnbr.gpkg")
calfire = st_read('Validation_Fire_Perimeters_2015_2024.shp') %>%
select(FIRE_NAME, geometry) %>%
rename(fire_name = FIRE_NAME) %>%
st_make_valid() %>%
st_transform(crs = st_crs(df_filtered))
calfire = st_read('Validation_Fire_Perimeters_2015_2024.shp') %>%
select(FIRE_NAME, geometry) %>%
rename(fire_name = FIRE_NAME) %>%
st_make_valid() %>%
st_transform(crs = st_crs(df_dnbr))
calculate_intersection = function(i) {
row = df_filtered[i, ]
cal_poly = calfire %>% filter(fire_name == row$fire_name)
if (nrow(cal_poly) == 0) return(NULL)
intersection = st_intersection(row, cal_poly)
# Handle case where there's no intersection
if (nrow(intersection) == 0 || all(st_is_empty(intersection))) {
return(data.frame(
fire_name = row$fire_name,
fire_days = row$fire_days,
percent_overlap = 0
))
}
# Sum all intersection areas (in case of multipart geometries)
intersection_area = sum(st_area(intersection))
calfire_area = sum(st_area(cal_poly))
pct = min(100, as.numeric(intersection_area / calfire_area * 100))
data.frame(
fire_name = row$fire_name,
fire_days = row$fire_days,
metric = row$metric,
percent_overlap = pct
)
}
# area overlap
df_filtered_dnbr = df_dnbr %>%
select(fire_name, fire_days, metric, geom) %>%
st_make_valid()
overlap_results_dnbr = map_dfr(1:nrow(df_filtered_dnbr), calculate_intersection) %>%
mutate(metric = 'dnbr')
calculate_intersection = function(i, df) {
row = df[i, ]
cal_poly = calfire %>% filter(fire_name == row$fire_name)
if (nrow(cal_poly) == 0) return(NULL)
intersection = st_intersection(row, cal_poly)
# Handle case where there's no intersection
if (nrow(intersection) == 0 || all(st_is_empty(intersection))) {
return(data.frame(
fire_name = row$fire_name,
fire_days = row$fire_days,
percent_overlap = 0
))
}
# Sum all intersection areas (in case of multipart geometries)
intersection_area = sum(st_area(intersection))
calfire_area = sum(st_area(cal_poly))
pct = min(100, as.numeric(intersection_area / calfire_area * 100))
data.frame(
fire_name = row$fire_name,
fire_days = row$fire_days,
metric = row$metric,
percent_overlap = pct
)
}
overlap_results_dnbr = map_dfr(1:nrow(df_filtered_dnbr), df_filtered_dnbr, calculate_intersection) %>%
mutate(metric = 'dnbr')
calculate_intersection = function(i, df) {
row = df[i, ]
cal_poly = calfire %>% filter(fire_name == row$fire_name)
if (nrow(cal_poly) == 0) return(NULL)
intersection = st_intersection(row, cal_poly)
# Handle case where there's no intersection
if (nrow(intersection) == 0 || all(st_is_empty(intersection))) {
return(data.frame(
fire_name = row$fire_name,
fire_days = row$fire_days,
percent_overlap = 0
))
}
# Sum all intersection areas (in case of multipart geometries)
intersection_area = sum(st_area(intersection))
calfire_area = sum(st_area(cal_poly))
pct = min(100, as.numeric(intersection_area / calfire_area * 100))
data.frame(
fire_name = row$fire_name,
fire_days = row$fire_days,
metric = row$metric,
percent_overlap = pct
)
}
# area overlap
df_filtered_dnbr = df_dnbr %>%
select(fire_name, fire_days, metric, geom) %>%
st_make_valid()
overlap_results_dnbr = map_dfr(1:nrow(df_filtered_dnbr), ~calculate_intersection(.x, df_filtered_dnbr))
df_filtered_rdnbr = df_rdnbr %>%
select(fire_name, fire_days, metric, geom) %>%
st_make_valid()
overlap_results_rdnbr = map_dfr(1:nrow(df_filtered_rdnbr), ~calculate_intersection(.x, df_filtered_rdnbr))
View(df_rdnbr)
View(overlap_results_rdnbr)
View(df_dnbr)
head(df_dnbr)
head(df_rdnbr)
View(calfire)
View(df_filtered_dnbr)
View(df_filtered_rdnbr)
